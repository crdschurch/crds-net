{
  "name": "livestreamtests",
  "version": "1.0.0",
  "description": "Test Live Stream message",
  "scripts": {
    "int_crossroads": "npx cypress run --config-file ./cypress/config/int_crossroads.json",
    "demo_crossroads": "npx cypress run --config-file ./cypress/config/demo_crossroads.json",
    "prod_crossroads": "npx cypress run --config-file ./cypress/config/prod_crossroads.json"
  },
  "author": "Sarah Henney",
  "license": "ISC",
  "dependencies": {
    "crds-cypress-config": "^1.0.1",
    "crds-cypress-contentful": "^2.0.0",
    "cypress": "^4.5.0",
    "moment-timezone": "^0.5.31",
    "slack-notify": "^0.1.7"
  },
  "devDependencies": {}
}
